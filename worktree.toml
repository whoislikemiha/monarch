# worktree.toml — generated by `workstree suggest`. DRAFT: verify before
# committing (create a throwaway worktree, run `workstree init` on it).

# Commands run in a fresh worktree, in order.
setup = [
  "npm ci",
]

# Untracked files copied from the source checkout (found git-ignored here).
# Review carefully: this list is usually secrets.
# copy = []

# Smoke check; nonzero exit = worktree NOT ready.
ready = "npm run build --if-present"
