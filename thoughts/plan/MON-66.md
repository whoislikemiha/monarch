# MON-66 — Archive shadows instead of deleting

Linear: https://linear.app/monarch-commander/issue/MON-66

## Summary

Today the sidebar shows every agent ever extracted because `db_get_agents` returns all rows unconditionally. The X button on an agent row kills the sidecar and removes the agent from the in-memory session list, but never touches the DB — so the agent reappears on next startup, dimmed as "standby". The operator-visible symptom is "why are all my old shadows here?".

The fix is to introduce **archive** as a first-class lifecycle state. A new nullable `archived_at` column on the `agents` table distinguishes active from archived shadows without deleting rows. The default sidebar view surfaces only active shadows. An Active / All toggle lets the operator see archived shadows inline (greyed out) and right-click → "Summon back" to resurrect them. Shadow identity, sessions, experience, and stats all persist unchanged across the archive/unarchive cycle.

No code path deletes DB rows. The existing `db_delete_agent` command is out of scope for this work but flagged for audit (see Open Questions).

## Relevant files and areas

### Rust / backend

- `src-tauri/src/db.rs`
  - `Database::new()` schema init + post-schema `ALTER TABLE` migrations, `db.rs:221-252`. New column is added here in the same pattern used for `context_window` (`db.rs:244`) and `ui_state` (`db.rs:248`).
  - `AgentRow` struct + `agents` table, `db.rs:137-151` (CREATE TABLE) and `db.rs:276-291` (struct).
  - Internal methods: `get_agents_internal` (`db.rs:464`), `upsert_agent_internal` (`db.rs:670`), `delete_agent_internal` (`db.rs:683`).
  - Tauri commands: `db_get_agents` (`db.rs:1076`), `db_upsert_agent` (`db.rs:1067`), `db_delete_agent` (`db.rs:1084`).
- `src-tauri/src/lib.rs` — command registration via `tauri::generate_handler!` and `--export-bindings` flag at `lib.rs:107-142`. New commands must be added here.
- `src-tauri/src/ws.rs` — WebSocket bridge that mirrors every Tauri command. `dispatch_command` at `ws.rs:177-433`, existing `db_get_agents` arm at `ws.rs:291-294`. Every new Tauri command needs a matching match arm.
- `src-tauri/src/agent.rs` — `kill_agent` at `agent.rs:1790-1796`, delegates to `AgentManager::kill` at `agent.rs:806-816`. No DB write today. Archive flow sits *around* kill, not inside it.

### Frontend

- `src/lib/bindings.ts` — auto-generated by tauri-specta. Regenerate via `cargo run -- --export-bindings` from `src-tauri/`. Do not edit by hand.
- `src/App.svelte`
  - `loadSavedAgents()` at `App.svelte:148-189` — the one and only place the frontend calls `db_get_agents`. Single integration point for filtering.
  - `killAgent()` at `App.svelte:532-540` — kills sidecar + closes tab + removes from local array. Needs to become "dismiss" flow (confirm → kill → archive → remove).
  - `loadUiState()` / `saveUiState()` at `App.svelte:191-214` / `App.svelte:262-270` — the existing `db_*_ui_state` persistence pattern used for `openTabs`, `activeTabId`, `sidebarCollapsed`, `theme`. The Active/All sidebar toggle plugs into this same mechanism.
- `src/lib/Sidebar.svelte`
  - X button + "Save as template" right-click menu already wired up. Right-click handler `openAgentMenu` at `Sidebar.svelte:46-61`; menu render at `Sidebar.svelte:173-194`. "Summon back" menu item slots in conditionally here.
  - `class:standby={agent.status === "stopped"}` styling at `Sidebar.svelte:137` already dims stopped agents. The archived styling can reuse / extend this visual language.
- Existing modal/dialog patterns: `SettingsDialog.svelte`, `SpawnDialog.svelte`, `ExtensionDialog.svelte`, `PromptEditor.svelte`, `ProjectEditor.svelte` — all follow the overlay + `role="dialog"` + Escape-to-close pattern. The Dismiss confirm dialog reuses this structure.

### Types / frontend domain

- `src/lib/types.ts` — the `Agent` frontend type. Needs an `archivedAt?: string` (or `archived: boolean`) field so the sidebar can style and the context menu can branch.

## What needs to change

### 1. DB migration (conceptual)

- Extend the `agents` table with a nullable ISO-timestamp column (`archived_at TEXT NULL`). NULL means active; any timestamp means archived. Timestamp is preferred over a boolean flag because it preserves the *when*, which is useful for sorting in the All view and for future "recently dismissed" UI.
- Add the `ALTER TABLE` in the same silent-ignore pattern as `context_window` so existing DBs migrate forward on next launch.
- Extend `AgentRow` struct in Rust and the corresponding TS binding with the new optional field.

### 2. Backend commands (conceptual)

- Extend `db_get_agents` with a flag to include archived rows (e.g. `include_archived: bool`, default false). This keeps one command rather than two, simpler for the frontend toggle. Default call signature stays compatible after bindings regenerate (default false at the Rust layer).
- Add `db_archive_agent(agent_id)` — sets `archived_at = CURRENT_TIMESTAMP`.
- Add `db_unarchive_agent(agent_id)` — sets `archived_at = NULL`.
- Register all three (new + modified) in `lib.rs` `generate_handler!`. Mirror the three in `ws.rs` `dispatch_command`.
- Regenerate `bindings.ts` via `cargo run -- --export-bindings`.

### 3. Frontend — loading and filtering

- `loadSavedAgents()` passes `include_archived: sidebarShowAll` to `db_get_agents`. The agent array gets both active and archived rows when All mode is on; only active when Active mode is on. `activeTabId` defaulting logic stays as-is (first agent) because it already guards against `length === 0`.
- Add an `archivedAt?: string` (or `isArchived: boolean`) field on the `Agent` type, populated from the DB row. Agents created in-session via `createAgent` start active (`archivedAt: undefined`).

### 4. Frontend — Dismiss flow (replacing current kill)

- The X button opens a small modal that confirms "Dismiss [shadow name]?" with explanatory copy ("Conversation history and stats are preserved. You can summon them back later."). Reuse the existing modal pattern (`role="dialog"` + overlay + Escape-closes).
- On confirm: kill sidecar (existing `kill_agent` Tauri command) → archive in DB (new `db_archive_agent`) → remove from local `agents` array → close tab if open.
- Copy changes: button tooltip `Dismiss`, confirm dialog title and action labels match.

### 5. Frontend — Sidebar toggle

- Add an Active / All segmented control to `Sidebar.svelte` header area. State lifted to `App.svelte` as a new reactive variable `sidebarShowAll` (default false), persisted via the existing `db_set_ui_state` / `db_get_ui_state` mechanism (same shape as `sidebarCollapsed`).
- When `sidebarShowAll` flips: re-run `loadSavedAgents()` (or fetch archived on demand and merge) so the sidebar has the full roster. Simpler: always fetch all when toggle is All, fetch only active when toggle is Active.
- Archived agents render with an additional CSS class (`class:archived`) that dims them more aggressively than standby and is visually distinct enough to signal "not currently in the roster".

### 6. Frontend — Summon back

- `Sidebar.svelte` right-click menu gains a conditional "Summon back" item when the clicked agent is archived. Wired through a new `onsummon` callback prop from the sidebar to App.svelte, mirroring the existing `onsavetemplate` / `onkill` pattern.
- On summon: call `db_unarchive_agent` → update the agent's local `archivedAt` to undefined → the agent is now in the active list. If the operator is in Active mode, it stays visible; if in All mode, it moves visually from archived to active styling.

### 7. Docs

- Update `CLAUDE.md` key files table if any new top-level file is added (none expected).
- Update `ONBOARDING.md` data model section to mention `archived_at` as part of the `agents` schema.

## Open questions

1. **Column name & type**. Spec says `archived_at TEXT NULL` (ISO timestamp). The Explore agent suggested `is_archived BOOLEAN`. Timestamp wins (preserves when), but worth confirming before committing.
2. **Single vs two commands for archive/unarchive**. Could be a single `db_set_agent_archived(id, archived: bool)` for symmetry, or two commands (`db_archive_agent`, `db_unarchive_agent`) for clarity. Minor — leaning two commands for call-site readability.
3. **`db_get_agents` signature**. Add `include_archived: bool` param (backwards-incompatible at the call site but regenerated bindings make it automatic) vs. add a separate `db_get_all_agents` command. Leaning single command with the flag.
4. **`db_delete_agent` reachability audit**. An existing `db_delete_agent` command sits at `db.rs:1084`. The acceptance criteria require "no code path permanently deletes agent rows". Before implementation, grep the frontend for any call to `db_delete_agent` / `deleteAgent` and either (a) confirm it's dead code and remove it, (b) confirm it's only gated behind some admin/debug path and leave it, or (c) note its existence in ONBOARDING.md. If it is live from some flow, we need a decision: keep it with a clear separation of intent, or gate it behind an explicit debug flag.
5. **Confirmation dialog copy**. "Dismiss [shadow name]?" with "Conversation history and stats are preserved" — this is my proposal. User should verify the voice matches the Shadow Army lore (e.g. "Return [name] to the shadow realm?" is more thematic but may feel silly).
6. **Toggle placement**. Two options for the Active / All control: (a) top of the sidebar next to the `+` button, (b) bottom of the sidebar as a footer control. Top is more discoverable; bottom feels lower-stakes / less cluttered. Leaning top.
7. **Should `kill_agent` remain a separate concept from archive?** Today killing just stops the sidecar. After this ticket, there's no UI to kill-without-archive. Is that OK, or do we want to keep a distinct "stop this shadow's process but keep it in the active roster" as a separate action? Likely out of scope for MVP, but worth naming.
8. **Archive auto-kill semantics**. If the operator dismisses an agent whose sidecar is already stopped (grade standby), should we still call `kill_agent`? Probably a no-op on Rust side — verify in `agent.rs:806-816` that kill on an already-killed agent is idempotent.

## Out of scope

- Bulk archive / multi-select
- Auto-archive of long-idle agents
- Archive search, filtering by date, or bulk views
- Permanent-delete UI (`db_delete_agent` stays as-is unless Open Question #4 surfaces a problem)
- Syncing archive state to Linear or other external systems
- Changes to how running agents' sidecar processes are managed beyond the existing `kill_agent` flow
