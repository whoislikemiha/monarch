# MON-119 — P6 Slice A research plan

## Summary

Land the backend substrate for P6 first-person quest reports. After this slice
Monarch has a `quest_reports` table, an ordered persistence variant for writing
into it via the existing single-consumer pipeline (MON-37), and Tauri commands
the captain UI (Slice C) and tests can use to insert and fetch reports.

Both the sidecar `complete_quest(report)` tool (Slice B) and any later
manual-edit path will route through the persistence variant added here. No
caller produces reports in this slice — that's Slice B. We are only landing
the data contract, the write path, and the read commands, so the rest of the
phase has somewhere to attach.

## Relevant files and areas

| File | Why it matters |
|------|----------------|
| `src-tauri/src/db.rs:125-641` | `init_schema` is the idempotent migration funnel. New post-launch tables go at the end alongside `quest_plan_items` (412), `quest_refs` (461), `memory_keeper_runs` (595). FK target for `distilled_by_keeper_run_id`. |
| `src-tauri/src/db.rs:3262-3500` | `list_quest_refs_internal` / `create_quest_ref_internal` — closest sibling shape: keyed by `quest_id`, returns a typed row, takes a payload struct, uses `crate::util::uuid_v4_simple` + `chrono_now`. Copy that style. |
| `src-tauri/src/db.rs:5474-5560` | `db_record_quest_event`, `db_record_manual_quest_event`, `db_create_quest_ref`, `db_list_quest_refs` — canonical command shape for `#[tauri::command] #[specta::specta]` + emit on the per-quest WS channel. |
| `src-tauri/src/db.rs:5762-5810` | `emit_quest_updated_notifications`, `emit_quest_ref_notification` — pattern for emitting on `quest-updated-{id}` and `quest-refs-{id}` after a write. Slice A will mint a `quest-report-{quest_id}` channel. |
| `src-tauri/src/db.rs:6080-6400+` | `#[tokio::test]` block at the bottom of the file — test helpers (`test_agent`, in-memory DB) live here. New tests go here. |
| `src-tauri/src/agent/persist.rs:60-210` | `PersistCommand` enum definition. New variant `WriteQuestReport { agent_id, payload }` lands here. |
| `src-tauri/src/agent/persist.rs:212-240` | `agent_id()` accessor — new variant needs an arm. |
| `src-tauri/src/agent/persist.rs:242+` | `apply()` is the single consumer's dispatch. New variant adds an arm that calls `db.write_quest_report_internal(...)` then emits on the per-quest channel via `agent::emit_event`. Pattern: `RecordQuestEvent` arm around line 419. |
| `src-tauri/src/lib.rs:262-330` | `tauri::generate_handler!` macro — three new command names added (insert + fetch + list). The duplicated list at 126 is the export-bindings entry point and needs the same additions. |
| `src-tauri/src/ws.rs` | WebSocket dispatch table. New arms for each new command. Browser-mode dev relies on parity. |
| `src/lib/bindings.ts` | Auto-generated by `cargo run -- --export-bindings`. Do not hand-edit; rerun the command after Rust DTO changes. |
| `CLAUDE.md:152` | The post-launch tables bullet — currently lists `projects, agent_templates, ui_state, agent_stats, agent_tool_usage, message_attachments, quest_nodes, quest_events, classifications`. Already missing `captain`, `captain_identity_versions`, `shadow_identity_versions`, `memories`, `memories_fts`, `memory_keeper_runs`, `quest_refs`, `quest_plan_items`, `agent_working_memory`. We are not retroactively fixing all of them in this slice — just adding `quest_reports` and any low-cost neighbors we trip over. |

## What needs to change

### Schema

A new `quest_reports` table inside the migration block at the end of
`init_schema`, placed after `memory_keeper_runs` since it forward-references
that table:

- **`id`** — text primary key (UUIDv4, matching `quest_refs` / `quest_events`
  conventions).
- **`quest_id`** — `TEXT NOT NULL REFERENCES quest_nodes(id) ON DELETE CASCADE`.
  When a quest is deleted, the report should go with it.
- **`payload`** — `TEXT NOT NULL` holding the structured JSON
  (`summary`, `outcome`, `decisions`, `learned`, `artifacts`, `open_threads`,
  `reflection`, `grade`). Slice A treats this as an opaque blob; Slice B
  defines the schema via the tool argument shape.
- **`author`** — `TEXT NOT NULL` (e.g. `"executor"`, `"captain"`).
  Matches the `author` convention on `quest_events`.
- **`created_at`** — `TEXT NOT NULL DEFAULT (strftime('%Y-%m-%dT%H:%M:%SZ','now'))`.
- **`distilled_by_keeper_run_id`** — `INTEGER REFERENCES memory_keeper_runs(id) ON DELETE SET NULL`,
  nullable. Slice D backfills this when the Keeper consumes the report.
- Index: `idx_quest_reports_quest` on `(quest_id, created_at)`.

Whether to allow multiple reports per quest is an **open question** (see
below); the schema does not constrain it.

### Persistence pipeline (MON-37)

A new `PersistCommand::WriteQuestReport { agent_id, payload }` variant in
`src-tauri/src/agent/persist.rs`. Mirrors `RecordQuestEvent`:

- New `WriteQuestReportPayload` request DTO (Serde/Specta-derived).
- Variant added to the enum.
- Arm added in `agent_id()`.
- Arm added in `apply()` that calls `db.write_quest_report_internal(payload)`
  and then emits on `quest-report-{quest_id}` via `agent::emit_event`.

No producer of this variant lands in Slice A — `event-handler.rs` is
untouched. Slice B will add the producer when it wires the
`complete_quest(report)` tool.

### Database helpers

In `src-tauri/src/db.rs`, modeled on `create_quest_ref_internal` /
`list_quest_refs_internal`:

- `write_quest_report_internal(&self, payload: &WriteQuestReportPayload) -> Result<String, MonarchError>`
  — validates `quest_id` and `payload` non-empty, mints UUID if absent,
  inserts, returns `id`.
- `get_quest_report_by_id_internal(&self, id: &str) -> Result<Option<QuestReportRow>, MonarchError>`.
- `list_quest_reports_for_quest_internal(&self, quest_id: &str) -> Result<Vec<QuestReportRow>, MonarchError>`
  — ordered by `created_at ASC` (matches `list_quest_refs_internal`).

`QuestReportRow` is a typed struct with Serde/Specta derives, mirroring
`QuestRefRow`. A `map_quest_report` helper for `rusqlite::Row -> QuestReportRow`.

### Tauri commands

Three new commands at the bottom of `db.rs`, alongside `db_create_quest_ref`:

- `db_save_quest_report(payload: WriteQuestReportPayload) -> Result<String, _>`
  — direct write via `write_quest_report_internal`, then
  `emit_quest_report_notification(quest_id, "saved", id)`. Mirrors
  `db_create_quest_ref`, not `db_record_quest_event`, because Slice B's
  sidecar writes will go through the `PersistCommand` path instead. This
  Tauri command is for tests and any later captain-initiated save.
- `db_get_quest_report_by_id(id: String) -> Result<Option<QuestReportRow>, _>`.
- `db_list_quest_reports_for_quest(quest_id: String) -> Result<Vec<QuestReportRow>, _>`.

A new `emit_quest_report_notification` helper alongside `emit_quest_ref_notification`,
broadcasting on `quest-report-{quest_id}`.

### Registration

- `src-tauri/src/lib.rs` — add the three command names to both the
  `tauri::generate_handler!` macro (runtime) and the duplicated list near
  line 126 (export-bindings).
- `src-tauri/src/ws.rs` — new arms in the dispatch table so browser-mode dev
  reaches the same commands.

### Bindings

`cargo run -- --export-bindings` from `src-tauri/`. Regenerated
`src/lib/bindings.ts` is the artifact; do not hand-edit.

### Tests

Inside the existing `#[tokio::test]` block at the bottom of `db.rs`,
following the `test_agent` helper pattern:

- Migration idempotency — run `init_schema` twice on the same in-memory DB, no
  error.
- Insert + fetch roundtrip — save a report, get-by-id returns matching row.
- List ordering — save two reports on the same quest with separated timestamps,
  list returns them in `created_at ASC`.
- FK to `quest_nodes` — saving with a non-existent `quest_id` fails.
- Nullable FK to `memory_keeper_runs` — insert with `distilled_by_keeper_run_id = NULL`
  succeeds (the default path in Slice A).

### Docs

- `CLAUDE.md:152` — add `quest_reports` to the post-launch tables list.
- No `ONBOARDING.md` change needed in this slice (no new architectural
  flow yet — Slice B/C/D will warrant those).

## Open questions

1. **One report per quest, or many?** The phase description in
   `roadmap.md:296-298` implies a single report at quest-close. Looking at
   `distillation.md` § "First-person quest report" should resolve this before
   touching the schema. If single, add a `UNIQUE (quest_id)` constraint and
   change Slice B's tool semantics from "insert" to "upsert". If many (e.g.
   the executor revises a draft, or a quest reopens and closes again),
   leave it unconstrained. **Recommendation: read the design doc first;
   default to many-allowed-but-rare with a unique index added only if the
   doc is explicit.**

2. **Should the Tauri save command (`db_save_quest_report`) go through the
   PersistCommand path instead of writing directly?** `db_record_quest_event`
   writes directly today; `db_create_quest_ref` also writes directly. Both
   precedents say "captain-initiated Tauri writes bypass the pipeline; only
   sidecar inbound events use PersistCommand." Slice A follows that. Worth
   confirming this is still the right shape in case write-ordering between
   the executor's report and a follow-on quest event matters — but I think
   it doesn't, because quest events and quest reports live in separate
   tables and aren't observed in lockstep.

3. **`author` column or `author_role` enum?** Existing `quest_events` uses
   a free-text `author` field (`"executor"` / `"captain"`). Following that.
   If P6's report ever needs to distinguish executor-self vs Architect-on-
   behalf-of-executor, that's a Slice B concern.

4. **Should we backfill `CLAUDE.md`'s post-launch tables list completely
   while we're touching it?** It's missed nine tables/views over the
   cognition phases. Doing the full backfill is a one-paragraph edit and
   keeps the doc honest, but it's scope creep relative to this slice.
   **Recommendation: do the full backfill in this slice. It's cheap, it's
   adjacent, and it's the kind of doc rot the docs-cleanup PR (#95) just
   addressed in a sibling area.**

## Out of scope reminders

- **No sidecar tool, no executor prompt change** — Slice B.
- **No UI** — the captain cannot see a report after this slice lands.
  Slice C wires the renderer into the P5 quest detail panel.
- **No Keeper consumption** — the Keeper still reads only the raw event
  stream slice on quest-close ticks. Slice D wires `quest_reports.payload`
  in as a second input.
- **No quest-status transition wiring** — closing a quest doesn't fail if
  no report exists. The "report-on-close" contract is enforced by the
  executor's prompt + tool semantics in Slice B, not by the schema.
- **No P5 quest detail panel changes** — `QuestTimelineTool.svelte` is
  untouched in Slice A.
- **No completed migrations backfill** for the partial `CLAUDE.md` table
  list outside the `quest_reports` row (unless open question 4 lands as
  "yes, full backfill").
